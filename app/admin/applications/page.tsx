'use client'
import { useMemo, useState } from 'react'
import { Search } from 'lucide-react'
import { AdminLayout, AdminPanel } from '@/components/admin/admin-layout'
import { applications } from '@/lib/data/school'
import { StatusBadge } from '@/components/layout/public-layout'
export default function ApplicationsAdminPage() { const [query, setQuery] = useState(''); const [status, setStatus] = useState('All statuses'); const filtered = useMemo(() => applications.filter((application) => `${application.name} ${application.id} ${application.program}`.toLowerCase().includes(query.toLowerCase()) && (status === 'All statuses' || application.status === status)), [query, status]); return <AdminLayout><div className="admin-top"><div><span className="eyebrow">Admissions</span><h2>Applications</h2><p>Review and manage incoming student applications.</p></div></div><AdminPanel title="All applications" description={`${filtered.length} applications shown`}><div className="table-controls admin-controls"><label className="search-field"><Search size={16} /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search applications" /></label><select value={status} onChange={(event) => setStatus(event.target.value)} aria-label="Filter status"><option>All statuses</option><option>Pending</option><option>Under review</option><option>Approved</option><option>Rejected</option></select></div><div className="table-wrap"><table><thead><tr><th>Applicant</th><th>Program</th><th>Status</th><th>Submitted</th><th>Action</th></tr></thead><tbody>{filtered.map((application) => <tr key={application.id}><td><strong>{application.name}</strong><small>{application.email}</small></td><td>{application.program}</td><td><StatusBadge status={application.status} /></td><td>{application.date}</td><td><button className="text-button">Review</button></td></tr>)}</tbody></table></div></AdminPanel></AdminLayout> }
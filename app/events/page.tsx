import { ArrowRight, CalendarDays, MapPin } from 'lucide-react'
import { InternalPage, PageFrame } from '@/components/layout/public-layout'
import { events } from '@/lib/data/school'
export default function EventsPage() { return <InternalPage><main className="events-page"><div className="container"><section className="events-hero"><div><span className="eyebrow">School calendar</span><h1>Make room for what matters.</h1><p>Open days, deadlines, celebrations, and moments that bring the ASPEJ community together.</p></div><CalendarDays size={76} strokeWidth={1} /></section><section className="events-list-section"><div className="events-section-heading"><div><span className="eyebrow">Coming up</span><h2>Important dates</h2></div><span className="events-count">{events.length} dates listed</span></div><div className="event-cards">{events.map((event, index) => <article className="event-card" key={event.id}><div className="event-number">{String(index + 1).padStart(2, '0')}</div><div className="event-date"><CalendarDays size={14} />{event.date}</div><h3>{event.title}</h3><p>{event.description}</p><span className="event-location"><MapPin size={13} /> ASPEJ campus</span><ArrowRight className="event-arrow" size={18} /></article>)}</div><div className="events-note"><CalendarDays size={19} /><div><strong>More events will appear here</strong><p>Connect the events API when official calendar data is available.</p></div></div></section></div></main></InternalPage> }

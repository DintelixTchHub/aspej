'use client'
import { useState } from 'react'
import { ArrowRight, CheckCircle2, Search, ShieldCheck } from 'lucide-react'
import { InternalPage, PageFrame, StatusBadge } from '@/components/layout/public-layout'
import { applications } from '@/lib/data/school'
export default function StatusPage() { const [query, setQuery] = useState(''); const result = applications.find((application) => application.id.toLowerCase() === query.trim().toLowerCase()); return <InternalPage><main className="status-page"><div className="container"><section className="status-hero"><div><span className="eyebrow">Applicant portal</span><h1>Follow your application with confidence.</h1><p>Use the reference number in your confirmation to check the latest status of your ASPEJ application.</p></div><div className="status-hero-mark"><ShieldCheck size={28} /><span>Secure<br />application lookup</span></div></section><section className="status-lookup-card"><div className="status-card-heading"><div className="status-card-icon"><Search size={18} /></div><div><span className="eyebrow">Check status</span><h2>Enter your reference number</h2></div></div><form className="lookup" onSubmit={(event) => event.preventDefault()}><div className="search-field"><Search size={17} /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="e.g. ASPEJ-260908-014" /></div><button className="button button-primary">Check status <ArrowRight size={14} /></button></form>{query && (result ? <div className="status-result"><CheckCircle2 size={20} /><div><strong>{result.name}</strong><span>{result.id}</span></div><StatusBadge status={result.status} /></div> : <div className="empty-state"><h3>No application found</h3><p>Check the reference number and try again.</p></div>)}</section><div className="status-help"><strong>Where can I find my reference number?</strong><span>It appears on the confirmation shown after you submit your application.</span></div></div></main></InternalPage> }
